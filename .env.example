# CanIEat Mobile App - Environment Configuration

# =====================================================
# Backend API Configuration (REQUIRED)
# =====================================================
# Your GCP Cloud Run backend URL (get from deployment)
EXPO_PUBLIC_BACKEND_URL=https://canieat-backend-abc123-uc.a.run.app

# API secret for backend authentication (get from: gcloud secrets versions access latest --secret=api-secret)
EXPO_PUBLIC_API_SECRET=your-api-secret-from-gcp

# =====================================================
# How It Works
# =====================================================
# App → Secure Backend → OpenAI
#   ✅ API key is protected server-side
#   ✅ Rate limiting enabled
#   ✅ Usage monitoring
#   ✅ Authentication required
# 
# =====================================================
# Getting Your Backend Values
# =====================================================
# 
# 1. Deploy backend (see ../backend/docs/DEPLOYMENT.md)
# 
# 2. Get backend URL:
#    gcloud run services describe canieat-backend \
#      --region=us-central1 \
#      --format='value(status.url)'
# 
# 3. Get API secret:
#    gcloud secrets versions access latest --secret=api-secret
# 
# 4. Copy values to .env file (this file, but without .example)
#
